## Nó único (hostname rabbitmq no compose): evita aviso "list of nodes does not contain the local node []"
cluster_formation.peer_discovery_backend = rabbit_peer_discovery_classic_config
cluster_formation.classic_config.nodes.1 = rabbit@rabbitmq

## RabbitMQ 4.3: feature ainda permitida até migração para Prometheus (ver log de depreciação)
deprecated_features.permit.management_metrics_collection = true
