# Generated by docker/kong/generate_config.py — do not edit by hand.
# Regenerate: python3 docker/kong/generate_config.py

_format_version: '3.0'
_transform: true
consumers:
- username: anonymous
  id: a0000001-0000-4000-8000-000000000001
- username: keycloak-athlos
  jwt_secrets:
  - algorithm: RS256
    key: http://localhost:8080/keycloak/realms/athlos
    secret: kong-rs256-placeholder-secret
    rsa_public_key: |-
      -----BEGIN PUBLIC KEY-----
      
      qwIDAQAB
      -----END PUBLIC KEY-----
  - algorithm: RS256
    key: http://localhost:8100/keycloak/realms/athlos
    secret: kong-rs256-placeholder-secret
    rsa_public_key: |-
      -----BEGIN PUBLIC KEY-----
      
      qwIDAQAB
      -----END PUBLIC KEY-----
  - algorithm: RS256
    key: https://athloshub.com.br/keycloak/realms/athlos
    secret: kong-rs256-placeholder-secret
    rsa_public_key: |-
      -----BEGIN PUBLIC KEY-----
      
      qwIDAQAB
      -----END PUBLIC KEY-----
plugins:
- name: cors
  config:
    origins:
    - http://athloshub.com.br
    - http://localhost:8100
    - http://localhost:3000
    methods:
    - GET
    - POST
    - PUT
    - PATCH
    - DELETE
    - OPTIONS
    - HEAD
    headers:
    - Accept
    - Accept-Version
    - Content-Length
    - Content-MD5
    - Content-Type
    - Date
    - X-Auth-Token
    - Authorization
    - X-Requested-With
    - X-Request-Id
    - X-User-Id
    - X-Keycloak-Sub
    - X-Keycloak-Roles
    - X-Keycloak-Email
    - X-Keycloak-Preferred-Username
    - Cache-Control
    - Connection
    - Keep-Alive
    exposed_headers:
    - Cache-Control
    - Connection
    - Content-Type
    - Date
    - Keep-Alive
    - Transfer-Encoding
    - X-Accel-Buffering
    credentials: true
    max_age: 3600
services:
# Next.js (NextAuth + rotas BFF em app/api/auth/*). Prioridade acima do auth-service:
# o prefixo amplo /api/auth no auth-service capturava /api/auth/session e enviava ao Keycloak → 404.
- name: nextjs-frontend
  url: http://host.docker.internal:3000
  protocol: http
  host: host.docker.internal
  port: 3000
  routes:
  - name: nextjs-api-auth-nextauth-bff
    protocols:
    - http
    - https
    paths:
    - ~/api/auth/session$
    - ~/api/auth/csrf$
    - ~/api/auth/providers$
    - ~/api/auth/error$
    - ~/api/auth/_log$
    - ~/api/auth/signout$
    - ~/api/auth/register$
    - ~/api/auth/refresh-token$
    - ~/api/auth/resend$
    - ~/api/auth/request-reset-password$
    - ~/api/auth/reset-password$
    - ~/api/auth/signin(?:/.*)?$
    - ~/api/auth/callback(?:/.*)?$
    strip_path: false
    regex_priority: 500
- name: auth-service
  url: http://host.docker.internal:8000
  protocol: http
  host: host.docker.internal
  port: 8000
  routes:
  - name: auth-public-auth
    protocols:
    - http
    - https
    paths:
    - ~/api/auth/login$
    - ~/api/auth/refresh$
    - ~/api/auth/logout$
    - ~/api/auth/keycloak/callback$
    - ~/api/auth/resend-verification$
    - ~/api/auth/google/url$
    - ~/api/auth/verify/[^/]+$
    - ~/api/auth/reset-password/[^/]+$
    strip_path: false
    regex_priority: 450
  - name: auth-public-health
    protocols:
    - http
    - https
    paths:
    - /api/health
    strip_path: false
  - name: auth-internal-validate-members
    protocols:
    - http
    - https
    paths:
    - /api/internal/validate-members
    methods:
    - POST
    strip_path: false
    regex_priority: 320
  - name: auth-internal-check-permission
    protocols:
    - http
    - https
    paths:
    - /api/internal/check-permission
    methods:
    - POST
    strip_path: false
    regex_priority: 320
  - name: auth-internal-check-permission-by-org-id
    protocols:
    - http
    - https
    paths:
    - /api/internal/check-permission-by-org-id
    methods:
    - POST
    strip_path: false
    regex_priority: 320
  - name: auth-internal-org-exists
    protocols:
    - http
    - https
    paths:
    - ~/api/internal/organizations/[^/]+/exists$
    methods:
    - GET
    strip_path: false
    regex_priority: 320
  - name: auth-public-team-invite-validate
    protocols:
    - http
    - https
    paths:
    - ~/api/teams/invites/[^/]+/validate$
    methods:
    - GET
    strip_path: false
    regex_priority: 200
  - name: auth-public-users
    protocols:
    - http
    - https
    paths:
    - /api/users
    strip_path: false
  - name: auth-public-orgs-list-get
    protocols:
    - http
    - https
    paths:
    - /api/organizations
    strip_path: false
    methods:
    - GET
    regex_priority: 0
  - name: auth-jwt-org-by-slug-get-optional
    protocols:
    - http
    - https
    paths:
    - ~/api/organizations/[^/]+$
    strip_path: false
    methods:
    - GET
    regex_priority: 150
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
        anonymous: a0000001-0000-4000-8000-000000000001
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: auth-jwt-users-me
    protocols:
    - http
    - https
    paths:
    - /api/users/me
    strip_path: false
    regex_priority: 200
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: auth-jwt-users-orgs
    protocols:
    - http
    - https
    paths:
    - /api/users/organizations
    strip_path: false
    regex_priority: 190
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: auth-jwt-orgs-me
    protocols:
    - http
    - https
    paths:
    - /api/organizations/me
    strip_path: false
    regex_priority: 180
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: auth-jwt-orgs-nested
    protocols:
    - http
    - https
    paths:
    - ~/api/organizations/.+/.+
    strip_path: false
    regex_priority: 170
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: auth-jwt-org-single-mutation
    protocols:
    - http
    - https
    paths:
    - ~/api/organizations/[^/]+$
    strip_path: false
    regex_priority: 160
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
    methods:
    - PUT
    - PATCH
    - DELETE
  - name: auth-jwt-org-post-root
    protocols:
    - http
    - https
    paths:
    - /api/organizations
    strip_path: false
    regex_priority: 155
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
    methods:
    - POST
  - name: auth-jwt-admin
    protocols:
    - http
    - https
    paths:
    - /api/admin
    strip_path: false
    regex_priority: 140
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: auth-jwt-teams
    protocols:
    - http
    - https
    paths:
    - /api/teams
    strip_path: false
    regex_priority: 130
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
- name: competitions-service
  url: http://host.docker.internal:8001
  protocol: http
  host: host.docker.internal
  port: 8001
  routes:
  - name: competitions-internal-teams
    protocols:
    - http
    - https
    paths:
    - /api/internal/teams
    methods:
    - POST
    strip_path: false
    regex_priority: 330
  - name: competitions-health
    protocols:
    - http
    - https
    paths:
    - /api/health
    strip_path: false
  - name: competitions-pub-modalities-get
    protocols:
    - http
    - https
    paths:
    - /api/modalities
    strip_path: false
    regex_priority: 200
    methods:
    - GET
  - name: competitions-pub-competitions-get
    protocols:
    - http
    - https
    paths:
    - /api/competitions
    strip_path: false
    regex_priority: 200
    methods:
    - GET
  - name: competitions-pub-scoreboard-ws
    protocols:
    - http
    - https
    paths:
    - ~/api/scoreboard/ws/[^/]+$
    strip_path: false
    regex_priority: 400
  - name: competitions-pub-scoreboard-get
    protocols:
    - http
    - https
    paths:
    - ~/api/scoreboard/[0-9a-fA-F-]{36}$
    strip_path: false
    regex_priority: 350
    methods:
    - GET
  - name: competitions-protected
    protocols:
    - http
    - https
    paths:
    - /api/competitions
    - /api/matches
    - /api/modalities
    - /api/scoreboard
    - /api/sport-teams
    - /api/rankings
    - /api/stats-rulesets
    - /api/sport-rulesets
    strip_path: false
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
- name: live-service
  url: http://host.docker.internal:8004
  protocol: http
  host: host.docker.internal
  port: 8004
  routes:
  - name: livestream-webhooks
    paths:
    - /api/webhooks
    protocols:
    - http
    - https
    strip_path: false
  - name: livestream-socket
    paths:
    - /socket.io
    protocols:
    - http
    - https
    strip_path: false
    preserve_host: true
  # OAuth callback: redirect do navegador sem Authorization (Google → Kong → live-service).
  # Deve ficar SEM plugin jwt; caso contrário 401 antes de chegar ao serviço.
  - name: livestream-google-calendar-oauth-callback
    protocols:
    - http
    - https
    paths:
    - ~/api/google-calendar/oauth/callback$
    methods:
    - GET
    strip_path: false
    regex_priority: 400
  - name: livestream-jwt
    paths:
    - /api/lives
    - /api/google-calendar
    protocols:
    - http
    - https
    strip_path: false
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
- name: notifications-service
  url: http://host.docker.internal:8003
  protocol: http
  host: host.docker.internal
  port: 8003
  routes:
  - name: notifications-internal-create
    protocols:
    - http
    - https
    paths:
    - /api/notifications/internal
    strip_path: false
    methods:
    - POST
    regex_priority: 300
    request_buffering: false
    response_buffering: false
  - name: notifications-user-api
    protocols:
    - http
    - https
    paths:
    - /api/notifications
    strip_path: false
    methods:
    - GET
    - POST
    - PUT
    - PATCH
    - DELETE
    - OPTIONS
    - HEAD
    regex_priority: 0
    request_buffering: false
    response_buffering: false
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
- name: notifications-sse-service
  url: http://host.docker.internal:8003
  protocol: http
  host: host.docker.internal
  port: 8003
  read_timeout: 3600000
  write_timeout: 3600000
  routes:
  - name: notifications-sse-stream
    protocols:
    - http
    - https
    paths:
    - /api/notifications/unread-count/stream
    strip_path: false
    preserve_host: true
    regex_priority: 400
    request_buffering: false
    response_buffering: false
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
- name: social-service
  url: http://host.docker.internal:8083
  protocol: http
  host: host.docker.internal
  port: 8083
  routes:
  - name: social-jwt-post-by-id-get-optional
    protocols:
    - http
    - https
    paths:
    - ~/api/social/posts/[^/]+$
    strip_path: false
    regex_priority: 125
    methods:
    - GET
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
        anonymous: a0000001-0000-4000-8000-000000000001
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: social-jwt-post-comments-get-optional
    protocols:
    - http
    - https
    paths:
    - ~/api/social/posts/[^/]+/comments$
    strip_path: false
    regex_priority: 125
    methods:
    - GET
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
        anonymous: a0000001-0000-4000-8000-000000000001
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: social-jwt-post-comments-post
    protocols:
    - http
    - https
    paths:
    - ~/api/social/posts/[^/]+/comments$
    strip_path: false
    regex_priority: 125
    methods:
    - POST
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: social-jwt-post-comment-mutation
    protocols:
    - http
    - https
    paths:
    - ~/api/social/posts/[^/]+/comments/[^/]+$
    strip_path: false
    regex_priority: 125
    methods:
    - PUT
    - DELETE
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: social-jwt-post-like-get-optional
    protocols:
    - http
    - https
    paths:
    - ~/api/social/posts/[^/]+/like$
    strip_path: false
    regex_priority: 125
    methods:
    - GET
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
        anonymous: a0000001-0000-4000-8000-000000000001
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: social-jwt-post-like-post
    protocols:
    - http
    - https
    paths:
    - ~/api/social/posts/[^/]+/like$
    strip_path: false
    regex_priority: 125
    methods:
    - POST
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: social-jwt-org-wall-posts-get-optional
    protocols:
    - http
    - https
    paths:
    - ~/api/social/organizations/[^/]+/posts$
    strip_path: false
    regex_priority: 125
    methods:
    - GET
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
        anonymous: a0000001-0000-4000-8000-000000000001
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: social-jwt-team-wall-posts-get-optional
    protocols:
    - http
    - https
    paths:
    - ~/api/social/teams/[^/]+/posts$
    strip_path: false
    regex_priority: 125
    methods:
    - GET
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
        anonymous: a0000001-0000-4000-8000-000000000001
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
  - name: social-pub--api-social-health-any
    protocols:
    - http
    - https
    paths:
    - /api/social/health
    strip_path: false
    regex_priority: 120
  - name: social-pub--api-social-info-any
    protocols:
    - http
    - https
    paths:
    - /api/social/info
    strip_path: false
    regex_priority: 120
  - name: social-pub--api-social-auth-public-any
    protocols:
    - http
    - https
    paths:
    - /api/social/auth/public
    strip_path: false
    regex_priority: 120
  - name: social-pub--api-social-feed-public-any
    protocols:
    - http
    - https
    paths:
    - /api/social/feed/public
    strip_path: false
    regex_priority: 120
  - name: social-pub--api-social-search-GET
    protocols:
    - http
    - https
    paths:
    - /api/social/search
    strip_path: false
    regex_priority: 115
    methods:
    - GET
  - name: social-pub--api-social-organization-profiles-GET
    protocols:
    - http
    - https
    paths:
    - /api/social/organization-profiles
    strip_path: false
    regex_priority: 115
    methods:
    - GET
  - name: social-pub--api-social-team-follow-count-GET
    protocols:
    - http
    - https
    paths:
    - /api/social/team-follow/count
    strip_path: false
    regex_priority: 115
    methods:
    - GET
  - name: social-pub--api-social-profile-GET
    protocols:
    - http
    - https
    paths:
    - /api/social/profile
    strip_path: false
    regex_priority: 115
    methods:
    - GET
  - name: social-pub--api-social-athlete-posts-GET
    protocols:
    - http
    - https
    paths:
    - /api/social/athlete/posts
    strip_path: false
    regex_priority: 115
    methods:
    - GET
  - name: social-pub--api-social-shares-user-GET
    protocols:
    - http
    - https
    paths:
    - /api/social/shares/user
    strip_path: false
    regex_priority: 115
    methods:
    - GET
  - name: social-pub--api-social-shares-count-GET
    protocols:
    - http
    - https
    paths:
    - /api/social/shares/count
    strip_path: false
    regex_priority: 115
    methods:
    - GET
  - name: social-pub--api-social-teams-GET
    protocols:
    - http
    - https
    paths:
    - /api/social/teams
    strip_path: false
    regex_priority: 115
    methods:
    - GET
  - name: social-pub--api-social-team-profiles-GET
    protocols:
    - http
    - https
    paths:
    - /api/social/team-profiles
    strip_path: false
    regex_priority: 115
    methods:
    - GET
  - name: social-pub--api-social-team-profiles-POST
    protocols:
    - http
    - https
    paths:
    - /api/social/team-profiles
    strip_path: false
    regex_priority: 114
    methods:
    - POST
  - name: social-pub--api-social-achievements-notify-POST
    protocols:
    - http
    - https
    paths:
    - /api/social/achievements/notify
    strip_path: false
    regex_priority: 114
    methods:
    - POST
  - name: social-pub--api-social-posts-GET
    protocols:
    - http
    - https
    paths:
    - /api/social/posts
    strip_path: false
    regex_priority: 113
    methods:
    - GET
  - name: social-pub--api-social-posts-POST-PUT-PATCH-DELETE
    protocols:
    - http
    - https
    paths:
    - /api/social/posts
    strip_path: false
    regex_priority: 112
    methods:
    - POST
    - PUT
    - PATCH
    - DELETE
  - name: social-jwt-catchall
    protocols:
    - http
    - https
    paths:
    - /api/social
    strip_path: false
    regex_priority: 0
    plugins:
    - name: jwt
      config:
        key_claim_name: iss
        claims_to_verify:
        - exp
        realm: athlos
    - name: post-function
      config:
        access:
        - |
          -- Injeta claims do JWT já validado pelo plugin jwt (RS256 / Keycloak).
          -- Não remove Authorization: os serviços podem repassar o Bearer para outros microsserviços
          -- sem revalidar a assinatura (validação ocorre apenas aqui no Kong).
          local token = kong.ctx.shared.authenticated_jwt_token
          if not token or type(token) ~= "string" then
            return
          end
          local parts = {}
          for part in string.gmatch(token, "[^%.]+") do
            table.insert(parts, part)
          end
          if #parts < 2 then
            return
          end
          local payload_b64 = parts[2]
          local pad = 4 - (#payload_b64 % 4)
          if pad ~= 4 then
            payload_b64 = payload_b64 .. string.rep("=", pad)
          end
          payload_b64 = payload_b64:gsub("-", "+"):gsub("_", "/")
          local raw = ngx.decode_base64(payload_b64)
          if not raw then
            return
          end
          local cjson = require("cjson.safe")
          local claims = cjson.decode(raw)
          if not claims or type(claims) ~= "table" then
            return
          end
          if claims.sub then
            kong.service.request.set_header("X-Keycloak-Sub", tostring(claims.sub))
          end
          if claims.email then
            kong.service.request.set_header("X-Keycloak-Email", tostring(claims.email))
          end
          if claims.preferred_username then
            kong.service.request.set_header("X-Keycloak-Preferred-Username", tostring(claims.preferred_username))
          end
          local ra = claims.realm_access
          if ra and type(ra) == "table" and ra.roles and type(ra.roles) == "table" then
            kong.service.request.set_header("X-Keycloak-Roles", table.concat(ra.roles, ","))
          end
- name: mediamtx-hls
  url: http://host.docker.internal:8888
  protocol: http
  host: host.docker.internal
  port: 8888
  routes:
  - name: hls-stream
    paths:
    - /live
    protocols:
    - http
    - https
    strip_path: false
    preserve_host: true
- name: keycloak-service
  url: http://host.docker.internal:8080
  protocol: http
  host: host.docker.internal
  port: 8080
  routes:
  - name: keycloak-routes
    protocols:
    - http
    - https
    paths:
    - /keycloak
    strip_path: false
    preserve_host: true
- name: frontend-service
  url: http://host.docker.internal:3000
  protocol: http
  host: host.docker.internal
  port: 3000
  routes:
  - name: frontend-routes
    protocols:
    - http
    - https
    paths:
    - /
    strip_path: false
    preserve_host: true
